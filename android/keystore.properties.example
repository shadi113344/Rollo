# Copy to keystore.properties and fill in (or run setup-release-keystore.ps1).
# NEVER commit keystore.properties or *.jks to git.

storeFile=rollo-release.jks
storePassword=your-store-password
keyAlias=rollo
keyPassword=your-key-password
